name: slide_countdown
description: Animation countdown timer for Flutter.
version: 0.0.1
homepage: null

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  stream_duration: ^1.0.0-dev.1

dev_dependencies:
  flutter_test:
    sdk: flutter
